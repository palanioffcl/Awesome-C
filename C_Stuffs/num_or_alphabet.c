#include <stdio.h>

main() {
        scanf("%d",&in);
        if(in > 'a' && in < 'z') {
                        printf("It is a alphabet");
        else if(in > 0 && in < 9) {
                        printf("It is an integer");
        else {
                        printf("It is an special character");
}
